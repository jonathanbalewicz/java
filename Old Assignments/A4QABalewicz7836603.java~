/**
 * A4QABalewicz7836603
 *
 * COMP 1020 SECTION D01
 * INSTRUCTOR    Heather Matheson
 * ASSIGNMENT    Assignment 4, Part A
 * @author       Jonathan Balewicz, 7836603
 * @version      November 20, 2018
 *
 * PURPOSE: 
 */
public class A4QABalewicz7836603 {
  public static void main(String[] agrs) {
    

    
  }
  public static String[] processFile(String[] bookings, int bookingsLength) {
    BufferedReader input;
    String[] words = new String[4];
    String line;
    int quantity;
    double price;
    String flavour;
      
    try {
      input = new BufferedReader(new FileReader(file));
      line = input.readLine();
      while (line != null) { // loop through lines
        words = line.split(",");

        if (words[0].equals("Coffee")) {
          quantity = Integer.parseInt(words[1]);
          size = words[2];
        } else if (words[0].equals("Donut")) {
          quantity = Integer.parseInt(words[1]);
          price = Double.parseDouble(words[2]);
          flavour = words[3];
        }
        line = input.readLine();
      }
      input.close();

    } catch (IOException ioe) {
      System.out.println(ioe.getMessage());
    }
  }
  public static void print(String[] seats, int seatsNum) {
    for (int f = 0; f < seatsNum; f++) {
      System.out.println(seats[f]);
    }
  }
}
class Order {
  private int quantity;

  public Order(int quantity) {
      this.quantity = quantity;
  }
  public void add(int amount) {
    quantity += amount;
  }
  public int getQuantity() {
    return quantity;
  }
  public String getName() {
    return name;
  }
  public String toString() {
    return quantity + " - " + name;
  }
}
class Coffee extends Order {
    private int size;
    
    public Coffee(int quantity, int size) {
        super(quantity);
        this.size = size;
    }
    
    
    
    
    
    
    
}
class Donut extends Order {
    private double price;
    private String flavour;
    
    public Coffee(int quantity, double price, String flavour) {
        super(quantity);
        this.price = price;
        this.flavour = flavour;
    }
    
    
    
    
    
    
    
}
